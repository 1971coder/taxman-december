/** @type {import('prettier').Config} */
const config = {
  singleQuote: false,
  semi: true,
  trailingComma: "es5",
  tabWidth: 2,
  printWidth: 100,
  arrowParens: "always"
};

module.exports = config;
